# Vite build output — regenerated by `npm run build` (in Chavah.NetCore/client).
# Served by the Vite dev server in Development and rebuilt by the Release publish
# target in Production (see Chavah.NetCore.csproj), so it is not tracked in git.
.vite/
assets/
vite-index.html
service-worker.js
workbox-*.js
